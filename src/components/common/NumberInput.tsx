import styled from "@emotion/styled";

const NumberInput = styled.input`
  width: 65px;
  height: 35px;
  text-align: center;
  padding: 0.4rem;
  font-size: inherit;
  border: none;
`;

export default NumberInput;
